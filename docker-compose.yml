services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - plectrumite-data:/app/server/data
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped

volumes:
  plectrumite-data:
